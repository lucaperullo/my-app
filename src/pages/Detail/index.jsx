import React from 'react';

export default function Detail() {
  // TODO : GET the query params by searching for a hook in react-router-dom library
  // TODO : use the id from the query params to fetch the statistics unsplash service based on image ID
  // TODO : Create a detail page by adding a Card with the image full width and the height should be 1/3 of the page, The statistics info should be shown under the image in an organized way ( use chakra components )
  return <div>index</div>;
}
